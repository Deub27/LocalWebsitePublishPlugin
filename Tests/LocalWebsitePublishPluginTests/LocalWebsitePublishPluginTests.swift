import XCTest
@testable import LocalWebsitePublishPlugin

final class LocalWebsitePublishPluginTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(LocalWebsitePublishPlugin().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
